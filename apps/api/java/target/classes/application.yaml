spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/menulink
    username: root
    password: 070239
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format-sql: true
    database: mysql
